# Experiment Results

Place experiment outputs here for reproducibility.

| Folder | Purpose |
|--------|---------|
| `baseline/` | No delay, no loss, no dropout — baseline metrics |
| `delay_tests/` | Asymmetric/symmetric link delay experiments |
| `dropout_tests/` | Packet loss and dropout rate experiments |

Document each run with: config used, CSV output, and notes.
